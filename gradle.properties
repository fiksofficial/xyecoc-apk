org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official

# Release signing credentials — DO NOT commit real values here.
# Provide them via environment variables in CI:
#   XYECOC_KEYSTORE_FILE, XYECOC_KEYSTORE_PASSWORD, XYECOC_KEY_ALIAS, XYECOC_KEY_PASSWORD
# ...or in your private ~/.gradle/gradle.properties (outside the repo):
#   xyecoc.keystoreFile=/absolute/path/to/release.keystore
#   xyecoc.keystorePassword=********
#   xyecoc.keyAlias=release
#   xyecoc.keyPassword=********
